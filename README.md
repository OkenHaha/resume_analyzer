# Resume Analyzer
This project is a simple Resume Analyzer built using FastAPI.

It prases your resume and compares you to a job description and give a json result of the comparision.


# Setup .env
NEBIUS_API_KEY=api_key

# Install Dependencies
`pip install fastapi uvicorn pydantic pdfplumber python-dotenv openai voyageai chromadb`